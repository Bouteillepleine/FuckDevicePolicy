pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        // libxposed (modern Xposed API) is published to Maven Central; the old
        // api.xposed.info repo hosted the legacy de.robv API and is no longer needed.
        mavenCentral()
    }
}
rootProject.name = "DuckPolicy"
include ":app"
