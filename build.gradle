// Top-level build file. AGP 8.13 / Kotlin 2.3 are the floor for compileSdk 36, which the
// libxposed `service` AAR requires.
plugins {
    id 'com.android.application' version '8.13.2' apply false
    id 'org.jetbrains.kotlin.android' version '2.3.0' apply false
}
