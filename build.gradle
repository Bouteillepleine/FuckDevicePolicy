// Top-level build file. If Android Studio prompts to upgrade AGP/Kotlin, accept
// its suggestion — these versions are a known-good baseline (Studio Koala+).
plugins {
    id 'com.android.application' version '8.6.0' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
}
